# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Office temp files
~$*.docx
~$*.xlsx
~$*.pptx

# Strategy/concept docx artifacts — keep them in Drive/Notion, not git
*.docx

# Regenerated by engage-prototype/_build/parse_master.py — re-run the script to rebuild
engage-prototype/assets/games.json

# Python build artifacts (the parser & generator are pure scripts — no need to ignore output)
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.env

# Local dev servers
.cache/
node_modules/

# Claude Code per-project state
.claude/
memory/
